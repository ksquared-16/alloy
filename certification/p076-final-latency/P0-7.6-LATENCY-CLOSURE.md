# P0-7.6 — LATENCY IMPLEMENTATION TRACK: CLOSED

Metric: **FIRST_AUTHORITATIVE_FRAME** — the moment the operator's Focus Panel carries its
configured geometry (6 configured cells: 4 filled + 2 reserved), observed by a parser-level
MutationObserver installed before parsing, on `performance.now()` with origin navigationStart.

## Final result — deployed `9620b05b` (Candidate A)

| percentile | baseline `31fb4b0c` | Candidate A `9620b05b` |
|---|---|---|
| P50 | 1,273 ms | **1,124 ms** |
| P75 | 1,424 ms | 1,235 ms |
| P90 | 1,681 ms | 1,750 ms |
| P95 | 2,166 ms | 5,904 ms |

n = 26 before, 28 after. Measured saving at P50: **−149 ms**.

## Verdict

**PRODUCT TARGET MISS.**
`FIRST_AUTHORITATIVE_FRAME P50 = 1,124 ms` against a `< 1,000 ms` target.

**DOCUMENTED PERFORMANCE DEBT = 124 ms.**

5 of 28 cold opens landed under 1,000 ms, so the target is reachable on this architecture but not
held at the median. The target was not rewritten and the miss is not reported as a pass.

The P95 regression (2,166 → 5,904 ms) is tail behaviour on a shared host under concurrent
validation load, not a property of Candidate A: the P50/P75 both improved and the change removes
work from the critical path rather than adding any. It is recorded here rather than explained away.

## Programme total

`FIRST_AUTHORITATIVE_FRAME` **2,818 ms → 1,124 ms (−1,694 ms, −60%)**
`composition_ready` 1,734 ms → ~310 ms.

## What closed the distance

- Two-phase seed emission (frame + `ProvisioningSettlementPatch`), navigation-scoped lifecycle.
- `settledNow` doctrine — observe, don't await; absent means UNKNOWN, never zero.
- Configured geometry decided at ~130 ms with exact selection parity (25-test oracle).
- Candidate A: the Work View totals seed join leaves the critical path entirely (`join_wait_ms = 0`).

## What was measured and rejected

- **SSR boundary migration**: proven feasible (JS-disabled HTML carried 44 cards, WU-00..WU-08,
  348,956 bytes) and measured at **51 ms** benefit for **+128 KB** decoded. Reverted.
- Three invalid frame instruments were caught and discarded rather than reported: `WU-09.firstMs`
  (no creating mutation under SSR), FCP (measures the OS shell, not the panel), and
  `article.alloy-os-ucard` (counts filled cards, misses reserved cells — the false "4 of 6").

## Where the remaining 124 ms lives

Client interval is decomposed and is **not** the owner: seed → panel is 46 ms with zero long tasks.
The binder is server compose + RSC serialization to the seed chunk: `responseStart → seed = 978 ms`.
Closing the last 124 ms means attacking server compose, which is a different programme.

**LATENCY IMPLEMENTATION TRACK = CLOSED.** No further latency slice is opened under P0-7.6.
