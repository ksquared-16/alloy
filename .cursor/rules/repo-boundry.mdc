---
description: Restrict Cursor to /Users/Kelly/Alloy only; never touch Alloy-Claude
alwaysApply: true
---

# Repo Boundary Rule

This Cursor workspace must only operate in:

/Users/Kelly/Alloy

Do not read, edit, diff, commit, push, or reference:

/Users/Kelly/Alloy-Claude
/Users/Kelly/Claude/Projects/Alloy

Canonical doc: `docs/platform/governance/agent-repo-boundaries.md`

Managed sprints (slot/worktree bootstrap): `docs/platform/governance/managed-sprint-operations.md`

`/Users/Kelly/Alloy-Claude` was **retired as an engineering root in July 2026**. It is no longer a specialist lane — it is not sanctioned for any repository work. Its history is preserved and lands by PR; nothing new is built on it.

Default branch behavior:
- Use `staging` for shared documentation/tooling/hotfix work.
- Use feature branches / managed worktrees for larger runtime work.
- Before work, run `alloy-root` (or confirm `pwd`, branch, `git status --short`, and the behind-count vs `origin/staging` by hand). Sharing a remote with the canonical repo proves nothing — a clone can be the right repository and still sit a thousand commits behind the right base.

**Both providers work from this repository**, separated by managed worktree and slot rather than by clone or topic. Claude sessions follow `CLAUDE.md` and the same managed-sprint doctrine.