# Slice 6 — Communications Convergence QA

## Environment

- Branch: `feat/work-items-v3-platform`
- Worktree: `/Users/Kelly/.cursor/worktrees/Alloy/wi3-platform`
- Org: Firefly Early Learning (`93667019-bd28-49b5-a688-acc9bb1e0a19`)
- Dev server: `cd web && npm run dev`

## Decision matrix

See `docs/sprints/08_2026/work-items-v3-platform/14-communications-convergence-matrix.md`.

**Selected lane:** Needs Reply (`needs_response`, `awaiting_parent_reply`).

## Fixture method

Firefly dev had **zero** authoritative Needs Reply threads at slice time (6 threads, none in needs-response state).

**Preferred setup (no customer data):**

1. Pick an existing org thread in Communications (example dev id: `4de7b8e8-ef5c-4609-b4b0-0e611dcd4600`).
2. Mark Needs Reply through the authoritative Communications triage action (`POST /api/admin/communications/conversations/{id}/triage` with `needs_response`), or restore-safe dev attention_state update on that thread only.
3. Confirm projection id: `communications:{threadId}` with **0** new `operational_tasks` rows.
4. Cleanup: triage `resolved` or restore prior `attention_state`.

## Scenarios

| # | Scenario | Expected |
|---|----------|----------|
| 1 | Work Items → Source Communications | Needs Reply threads project once |
| 2 | Select projected row | Provenance `Generated by Communications → Family → Needs Reply` |
| 3 | Open in Communications | Exact thread selected; no New Message reset |
| 4 | Authoritative reply / resolve | Projection disappears after refresh |
| 5 | Communications → View in Work Items | Opens queue with exact synthetic row selected |
| 6 | Processing + BP regression | Unchanged |

## Automated validation (completed)

```bash
cd web && npm run test -- tests/workItems/
NODE_OPTIONS=--max-old-space-size=8192 npx tsc --noEmit --incremental false
```

## Manual QA status

**BLOCKED BY DATA** — no dev Needs Reply thread without a one-time triage/fixture step on an existing org thread.

## Screenshots

Pending manual capture after fixture step. Target paths:

- `01-work-items-overview.png`
- `02-communications-source-enabled.png`
- `03-communications-projected-row.png`
- `04-communications-detail.png`
- `05-open-in-communications-thread.png`
- `06-comms-view-in-work-items.png`
- `07-work-items-selected-from-comms.png`
- `08-queue-after-resolution.png`
- `09-comms-mine-empty-state.png` (if applicable)

## Defects found/fixed during implementation

- D-6-1: Duplicate comms rows on refresh → dedupe in `mapCommunicationsQueueToWorkItemRows`.
- D-6-2: `countTasksForView` missing `userId` for Mine/Unassigned → pass `userId` from `MyTasksPanel`.
- D-6-3: False Due Today from last-activity timestamp → comms rows excluded from due_* views; queue shows last activity label.

## Known gaps

- Live authenticated round trip not executed in-agent (fixture/triage write blocked in automation).
- Assignment → Mine depends on Communications `assigned_user_id`; unassigned threads correctly appear under Unassigned, not Mine.

## Cleanup status

No persistent fixture applied in this slice (dev threads unchanged).
