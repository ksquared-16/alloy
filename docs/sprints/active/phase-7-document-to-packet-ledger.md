---
owner: platform
status: active
last_reviewed: 2026-07-24
---

# Phase 7 — Document-to-Packet: Execution Ledger

Single source of execution truth. Plan:
[`../../platform/planning/phase7-document-to-packet-plan.md`](../../platform/planning/phase7-document-to-packet-plan.md).

Branch `agent/claude/1-phase7-document-packet-journey` (managed wt1, slot 1, port 3011). Base: `origin/staging`.
Do not push/merge until explicitly instructed.

**Server discipline note (accepted):** on 2026-07-24 the wt1 :3011 server had been reclaimed under the 3/3 server cap; `wt3-runtime-v1-polish` (a closed/merged initiative) was stopped to free a slot and wt1 restarted. Reversible; wt3 can be restarted. Keep the wt1 managed server healthy for certification; do not rely on unmanaged servers.

## Slice status

| Slice | Status | Acceptance outcome (walkable) |
|---|---|---|
| 0 — Fidelity generation + native signing proof | **done** | Real PDF filled + signed + flattened, immutable artifact + audit, automated + artifact verification (6/6 tests) |
| 1 — Source doc → reviewed published form (+OCR) | **DONE — CERTIFIED (Stage A + Stage B image + Stage B scanned-PDF, all green in-server)** | Upload real PDF, correct ≥1 field, preserve a consent section, publish; repeat with a scanned image AND a scanned PDF |
| 2 — Packet composition + requirement responsibility | **DONE — CERTIFIED** (operator product green; live completion + Processing handoff green, one case + idempotent; distinct multi-guardian execution → Slice 3) | Compose + configure responsibility per requirement in operator language; household preview; blocking validation; persist; launch; complete via real submission → one Processing Case |
| 3 — Participant Conversation Runtime + distinct multi-guardian completion | not started (next) | Per-recipient attribution/steps unblock independent completion by two guardians of the same requirement; parent pastes info, confirms interpretation, uploads, resumes cross-session, reaches document review |
| 4 — Generation + review + signing in journey | not started | Guardian reviews + signs a faithful completed doc; flattened PDF + evidence retrievable |
| 5 — Submission + unified Mailroom review | not started | Completed packet reviewed as one coherent case |
| 6 — Targeted correction round-trip | not started | Request correction of one requirement, resubmit, receive back in same review |
| 7 — Approval, commit, filing, copies, resend | not started | Commit + file + retrieve from child record + send copy + simulate failure + resend |
| 8 — Full certification | not started | 23-step acceptance journey + full certification gate |

## Checkpoint log

### 2026-07-24 — Sprint bootstrap
- Accepted plan saved (`phase7-document-to-packet-plan.md`). Execution ledger created.
- Preserved commits: `ec4954888` (distribution-link/folder/embed addendum), `58280d1ac` (architecture-freeze closeout).
- Branch reconciliation onto latest `origin/staging`: see checkpoint below.

### 2026-07-24 — Slice 0: fidelity generation + native signing proof — DONE
- **Outcome now real (engine-level):** a source PDF is filled with fidelity (original-layout AcroForm fill + coordinate overlay), a signature (typed / drawn PNG / initials) is placed at the correct location, the output is flattened into an **immutable** signed artifact (0 fillable fields), with hashed source→populated→signed **version lineage** and per-signature **audit evidence** (kind, typed name, drawn-asset flag, signer id, intent-acknowledged timestamp, hashed IP, placement, provenance). Intent acknowledgement is a hard gate.
- **Files:** `web/lib/forms/pdf/generation/{types,fidelityEngine,enrollmentFixture}.ts`; test `web/tests/forms/pdf/fidelityEngine.test.ts`; dep `pdf-lib@^1.17.1`.
- **Tests/evidence:** 6/6 vitest green. Openable artifacts written to scratchpad `phase7-slice0-evidence/` (source/populated/signed PDFs + `lineage.json`): 3 distinct SHA-256s, `signed_is_flattened: true`, all 4 fields applied / 0 missed, typed-signature audit row. Fidelity of the unflattened doc verified via the form API; fidelity of the flattened signed doc verified via text extraction (filled values + signature present).
- **Defects reaffirmed (targets for later slices):** the production generator is still the `stubFormPdfGenerator` (plain-text) — Slice 4 swaps in this engine; the participant "drawn signature" is still a UUID field — Slice 4 wires real capture.
- **Deferred non-blockers:** pixel screenshot (browser pane blocked localhost/file:// this session — openable PDFs stand in); persistence of versions/audit to Documents + `form_submission_signatures` (Slice 4/7).
- **Non-goals honored:** no production UI, no storage/DB wiring — engine + proof only.
- **Commit:** _(below)_ · **Next:** Slice 1 (source document → reviewed, published form; + OCR path before final cert).

### 2026-07-24 — STAGE A CERTIFIED GREEN (authenticated Playwright, full operator journey)
- **`playwright/tests/phase7-document-to-form-native.spec.ts` PASSES (1.5m).** Real authenticated operator journey through the product surfaces: service-role auth → Digital Mailroom Work → Queue → intent-modal upload of a real multi-section enrollment PDF → processing-case creation → open case → **extraction review** (operator-language confidence visible) → **rename a field** → **change a field type before creation** → **map a field to a canonical field** → **mark a field packet-only** → **ignore a field** → **section dispositions: static reference / acknowledgement / signature** → generate → form builder → **preview** → **save draft** → **publish** → retrievable in Studio.
- **Schema + lineage verified (dispositions are NOT cosmetic):** published version schema field types = `text,date,text,text_block,text_block,boolean,text_block,text,signature` — static-reference + acknowledgement sections emit preserved `text_block`, acknowledgement emits a required `boolean`, the signature section emits a `signature` field; instructional/consent text preserved. Source→published lineage asserted (form_id + published version retrievable via the Forms API).
- **Product fixes that unblocked it (all real, shipped):** (1) form-setup format detection uses the document's real `originalFilename` (not the extension-less display label) — the "Automatic question detection is not available" dead-end for valid PDFs [`bcd7a1fb4`]; render path fully traced (branch=document-setup, draft loads, review rows render). Cert harness handles the fresh-case auto-detect (reload-to-load-persisted-draft), neutralizes the floating BOS rail overlay's pointer interception, and drives the "Generate anyway" (unresolved) path.
- Screenshots: `web/docs/sprints/active/phase-7-evidence/stage-a-native/` (01-authenticated, 02-uploaded, 02b-setup-column, 03..08 review/dispositions/preview/publish). Source typecheck clean; unit tests green.
- **Next:** Stage B — governed OCR scanned-document path + its authenticated cert (OCR engine already built + proven offline; upload branch wired).

### 2026-07-24 — Setup-entry DIAGNOSED + product defect FIXED (verified); Stage A/B Playwright green NOT reached (harness render wall)
- **Part 1 — canonical setup-entry truth (traced statically + with a diagnostic spec):** ProcessingModal → DigitalMailroomShell (Work/Overview + Queue tabs) → PosProcessingWorkspace (Queue view) → on case selection `usePosCase` loads detail; `isDocumentCase` (primary source kind document/upload/recreated_document) → renders `PosTemplateSetupColumn`. With no `form_draft_preview` yet, that column AUTO-DETECTS when `shouldAutoDetect = generate_form && sourceCapabilities.questionDetection`. DB truth confirmed the uploaded case is correct (`case_type=form_like_document`, primary kind `document`, intent `generate_form`).
- **Part 2 — the entry was BROKEN; fixed the smallest correct product defect.** `PosTemplateSetupColumn` derived the source format from `primary.display.label` — a human display name ("Upload 1784… — 07/24/2026") with **no file extension** → `detectProcessingSourceFormat` misresolved → `questionDetection=false` → the "Automatic question detection is not available" dead-end (no setup entry) for a valid PDF. **Fix:** expose the raw `originalFilename` on `SourceDisplayDescriptor` (the read model already fetched it) and detect format from it. Files: `readModel/types.ts`, `readModel/processingCaseReadModelDb.ts`, `readModel/sourceDisplayResolvers.ts`, `PosTemplateSetupColumn.tsx`. **VERIFIED:** the dev server now fires `POST /form-draft` (auto-detect) on opening a document case — which it did NOT before the fix.
- **Stage A/B Playwright green — NOT reached.** In the authenticated Playwright modal harness, after case selection the setup review panel (`PosTemplateSetupColumn`) is not observable in the DOM (center detail renders empty). Reproduces with warm routes AND with BOS closed → not cold-compile, not occlusion; a modal/case-detail render quirk under the test harness needing interactive DOM/React-state debugging. 9 native-PDF attempts + 3 diagnostic runs. `phase7-document-to-form-native.spec.ts` carries an honest `test.fixme` (verified up to auth + intent-modal upload + case creation + queue nav); `phase7-setup-entry-diagnostic.spec.ts` retained `test.skip` for future debugging. Stage B OCR cert not authored (blocked behind the same harness render).
- **Honest status:** Parts 1 & 2 of the mandate DONE (flow diagnosed; real product defect fixed + log-verified). Stage A/B UI green certification NOT achieved — blocked on a Playwright-harness render issue for the case-detail panel, not a product defect. Everything else (Stage A capability, Stage B OCR engine) unit-tested + typecheck-clean.

### 2026-07-24 — Stage A capability COMPLETE + unit-certified; Stage B OCR engine built; UI Playwright cert INCOMPLETE
- **Stage A capability (built, unit-tested, typecheck-clean):** section-disposition selector wired end-to-end — UI selector (`ProcessingQuestionReviewList`) → `PosTemplateSetupColumn` state → `section_dispositions` in the save payload → `/form-draft/save` → `buildManualFormDraft` (attaches disposition + **preserves section prose as static_text — no silent data loss**) → `draftFormToFormSchemaV1` emits the right controls (preserved `text_block` + `boolean`/`file_ref`/`signature`), all schema-valid. Disposition is NOT cosmetic (unit test asserts emitted schema per disposition). Plus **pre-create field-type editing** and **operator-language confidence** (High confidence / Review recommended / Needs attention). Fixed a latent `MintArgs` type error the typecheck caught.
- **Stage B OCR engine (built, proven offline):** `lib/pos/processingCase/structure/ocrExtract.ts` — tesseract.js (WASM, self-contained; core bundled; `eng.traineddata` in-repo at `ocr-data/`, loaded via local `langPath` — no CDN). `looksLikeImage` detection + `ocrImageBytes` (dynamic import; confidence + per-word bbox provenance; low-confidence flag at <70). Wired into the documents/upload route (image → OCR → extracted_text + `extraction_provider` + `metadata.ocr_*`). Proven: OCR'd the scanned enrollment fixture offline at confidence 66. **Dependency added: `tesseract.js@^5`** (+ `pdf-lib@^1.17.1` from Slice 0). Runtime: OCR is slow/CPU-heavy; runs best-effort inline (production should move to an async worker); deployment must ship `ocr-data/eng.traineddata` (~4MB); failure → null result + honest state (never silent empty).
- **Certification (Option B, authenticated headless Playwright) — PATH PROVEN, full cert INCOMPLETE:** `playwright/tests/phase7-document-to-form-native.spec.ts`. VERIFIED through real surfaces across 5 iterations: service-role auth ✓, Digital Mailroom Work modal ✓, **intent-modal upload of a real multi-section enrollment PDF ✓**, processing-case creation ✓, queue navigation ✓ (screenshots `web/docs/sprints/active/phase-7-evidence/stage-a-native/01,02`). **REMAINING BLOCKER:** after opening the imported case, the automated run does not reach the document form-SETUP review panel (`PosTemplateSetupColumn`) — the case opens as a regular review case; the setup/"detect questions" entry needs one more product step identified **interactively** (blind 8-min headless iterations were insufficient to locate it). Test marked `test.fixme` with a precise note (NOT a red-blocker, NOT false-green). Stage B (OCR) Playwright cert not yet authored (blocked behind the same setup-panel entry).
- **Fixtures added:** `tests/fixtures/processing/enrollment-multisection-acroform.pdf` (4-page AcroForm, page-per-section), `enrollment-scanned.png` (rasterized via macOS `sips` — no rasterizer dep).
- **Honest status:** the Stage A/B *capabilities* are real, unit-tested, and typecheck-clean; the *UI acceptance certification* is not complete — the operator-visible reviewed→published journey is not yet demonstrated green end-to-end in the automated test. Not declaring the slice complete.
- **Next (interactive):** identify the "set up / create form from this document" entry step in the case work column, wire it into the cert test, get Stage A green, then author Stage B OCR cert. Commits below.

### 2026-07-24 — Slice 1/2 (source doc → published form): backend increment DONE; UI + browser-cert BLOCKED
- **Delivered (production, tested):** section **disposition** model + **static/consent/signature text preservation** through the draft→schema pipeline. `recommendSectionDisposition` classifies each section (fields / static_reference / acknowledgement / upload / signature / initials / generated) with rationale + confidence and preserves the prose field-extraction otherwise discards; `draftFormToFormSchemaV1` now emits the right FormSchemaV1 constructs per disposition (preserved `text_block` + `boolean`/`file_ref`/`signature` control), all validating against the live zod schema. This is the deterministic core the operator review UI will drive.
- **Files:** `web/lib/pos/processingCase/formDraft/sectionDisposition.ts` (new); `web/lib/pos/processingCase/formDraft/types.ts` (+ `disposition`, `static_text` on section); `web/lib/pos/processingCase/formDraft/draftFormToFormSchemaV1.ts` (disposition-aware). Test `web/tests/pos/sectionDisposition.test.ts`.
- **Tests/evidence:** new suite green; existing draft/schema suites green (44/45 across the 6 draft tests). Every disposition-mapped schema passes `validateFormSchema`, incl. a mixed enrollment draft (fields + upload + acknowledgement + signature).
- **Discovered pre-existing defect (NOT mine):** `deriveDocumentTitle` classification-label test fails on the reconciled staging base (`immunization_record` → "Child Medical Examination Report" vs expected "Immunization Record"); reproduces with my edits stashed. Unrelated to this slice; left untouched (scope discipline) — flagged for a separate fix.
- **BLOCKER (gates full acceptance of this and every operator slice):** the slice's acceptance requires **browser certification of the authenticated operator authoring experience** (upload → correct → publish). The in-app browser pane refuses `localhost` "by policy" this whole session, and the operator form-studio needs an authenticated Supabase session the pane cannot hold (auth = Playwright/service-role magic-link; credentials must not be handled here). Remaining operator-visible work that depends on this: the section-disposition selector + pre-create field-type editing in `ProcessingQuestionReviewList`, confidence visualization, the OCR scanned-document path, and publish-in-browser. **Awaiting a certification-path decision** (options in the checkpoint reply).
- **Commit:** _(below)_ · **Next:** unblock certification path, then finish the operator UI for this slice.

### 2026-07-24 — Certification path PROVEN + pre-create type editing (slice continuing)
- **Certification path proven (Option B):** authenticated headless Playwright runs against the managed :3011 server. The service-role helper (`ensureAdminPlaywrightSession`, env sourced from canonical `.env.local`, secrets never printed) successfully authenticates and the operator workspace loads. Evidence: `docs/sprints/archive/07_2026/processing-form-composer-v1-screenshots/01-workspace-authenticated.png`.
- **Existing acceptance surface confirmed:** `playwright/tests/processing-form-composer-v1-e2e.spec.ts` already drives upload real AcroForm PDF → detect → review (subject/canonical binding, packet-only, ignore, name-split) → generate → builder → save → preview → **publish**, with screenshots. That is most of this slice's acceptance path, already authored.
- **Delivered this increment:** **pre-create field-type editing** — the review inspector's "Answer type" is now an editable select (`review-type-<id>`) that persists through the existing save→create→schema path (save already carries `type`). Satisfies acceptance item "change one field type before creation."
- **Friction (not hard blockers, recorded):** (1) my :3011 server had been reclaimed under the 3/3 server cap; I stopped the closed-initiative `wt3-runtime-v1-polish` server to free a slot and restarted wt1 — reversible, restartable. (2) The existing E2E is **brittle under cold Next dev-compile** (first authenticated hit shows a "Thinking." spinner; fixed 60s waits + a stale `getByRole('dialog',{name:/Processing/i})` assertion time out). Not an auth/product break — needs a robust acceptance test (warm route, resilient selectors) which I'll author for this slice's cert.
- **Remaining to complete this slice (next turn(s), no stubs):** section-disposition selector wired UI (backend core already committed `beca88140`) + persistence through save/create; confidence/quality visualization surfaced explicitly; governed **OCR path** (image-only detection → server-side OCR → confidence/provenance → same review flow → no auto-publish low-confidence); a robust authenticated Playwright acceptance test for the full operator path + a second scanned/OCR path, with screenshots + trace/video-on-failure.
- **Commit:** _(type-editing below)_ · **Next (within this slice):** robust Playwright acceptance test + section-disposition UI wiring + confidence viz + OCR.

### 2026-07-25 — STAGE B CERTIFIED GREEN + Slice 1 CLOSED (governed OCR: scanned image AND scanned PDF, in-server)

**Slice 1 is complete and certified.** The same operator authoring journey now accepts scanned/image sources end-to-end, governed, and proven in the shipping Next server (not just a library spike).

- **Two authenticated Playwright certs pass green (`playwright/tests/phase7-document-to-form-ocr.spec.ts`):**
  - *Stage B image* — upload a scanned enrollment **image** → OCR (no native text/fields) → OCR-derived review (banner + operator-language confidence) → correct a field → publish → lineage. `STAGE_B_PASS` `ocr_source={method:ocr-tesseract-v5, confidence:73, source_kind:image}`.
  - *Stage B scanned PDF* — upload an **image-only PDF** → detected as non-native → **rasterized server-side (mupdf)** → OCR → same review + publish → `source_kind=scanned_pdf`. `STAGE_B_PDF_PASS` `ocr_source={…, confidence:91, source_kind:scanned_pdf}`. Screenshots in `web/docs/sprints/active/phase-7-evidence/stage-b-ocr/`.
- **Governed OCR product integration (same journey, no separate OCR workspace):** eligibility (`questionDetectionAvailable = capability || OCR-derived`) so OCR docs are form-setup eligible; detection sources distinguished in **operator language** — "Detected from fillable PDF" / "Detected from document text" / "Detected using OCR — review recommended" / "We could not reliably read this page" (no "AcroForm" jargon leaking); an amber (non-alarming) OCR banner with confidence in operator language (High confidence / Review recommended / Needs attention / Could not determine) + numeric % as supporting detail + a "verify each field before publishing" policy line. Governed publishing: low-confidence/unresolved findings gate direct publication behind an explicit **"Generate anyway"** confirmation; original OCR text preserved; operator corrections recorded on top (not overwriting source); no arbitrary global threshold (field-/document-level quality + documented 70% low-confidence flag).
- **Deployment-critical fixes (the mandate's production-verification core), each found by running the real server:**
  1. `tesseract.js` bundled by webpack → its Node worker child resolved to `/ROOT/node_modules/.../worker-script/node/index.js` (missing) → upload hung 107s–2.9min. **Fix:** `serverExternalPackages += tesseract.js` (load from node_modules; worker path intact). Upload dropped to ~8s.
  2. pdf.js page rendering AND image extraction (`unpdf.renderPageAsImage` / `extractImages`) throw **"Cannot transfer object of unsupported type"** in the Next server runtime (worker/transfer path breaks once bundled; works only in raw node/tsx) → scanned PDFs never OCR'd. **Fix:** replaced pdf.js rasterization with **`mupdf`** (self-contained single-threaded WASM — no worker, no native addon, no canvas; renders reliably in-server AND serverless). Confidence rose 73→91.
  3. OCR provenance dropped on the operator-review **save** (draft rebuilt without `ocr`) → published form missing `ocr_source`. **Fix:** single-source derivation `ocrProvenanceFromDocument(document row)` applied on both the detector path and the save path; `ocr` now round-trips through `parseStoredFormDraftPreview`.
  4. Runtime-loaded assets invisible to Next's file tracer (would be missing on Vercel): `outputFileTracingIncludes` for the upload route now force-includes `ocr-data/**` (eng.traineddata), `tesseract.js` worker-script + `tesseract.js-core` WASM, and `mupdf/dist/*.wasm`.
- **Worker lifecycle / bounds (memory + duration):** one tesseract worker per document, reused across all PDF pages, `terminate()` in `finally`; mupdf `Document`/`Pixmap`/`Page` all `destroy()`'d; caps `OCR_MAX_PDF_PAGES=8`, `OCR_MAX_INPUT_BYTES=25MB` (over-cap refused, over-page tail flagged `truncated`). All best-effort: any failure → null → the document is still stored and the operator sees an honest "couldn't read" state, never a silent empty form.
- **Files:** `lib/pos/processingCase/structure/ocrExtract.ts` (image + scanned-PDF engine, mupdf rasterize), `lib/pos/processingCase/formDraft/ocrDraftProvenance.ts` (new, single source of OCR provenance), `formDraft/{buildFormDraftForCaseSafe,createFormFromCaseDraft,formDraftPreviewDb,detectionModeLabel,types}.ts`, `app/api/admin/documents/upload/route.ts` (image + scanned-PDF branches, shared `applyOcrDocumentUpdate`), `app/api/admin/processing/cases/[caseId]/form-draft/save/route.ts` (preserve OCR), `readModel/*` (OCR threaded to descriptor), `PosTemplateSetupColumn.tsx` (banner + eligibility), `next.config.ts` (externals + tracing). Deps added: `mupdf@^1.28` (+ `tesseract.js@^5` from earlier). Fixtures: `tests/fixtures/processing/enrollment-scanned.pdf` (image-only, no native text). Tests: `tests/pos/ocrExtract.test.ts` (classification + tones + bounds + over-cap guard).
- **Tests/evidence:** Stage A native cert green; Stage B image + scanned-PDF certs green (combined single run: **3 passed**); OCR + section-disposition + formComposer unit suites green (19/19 across those files); source + test typecheck clean (0 errors); **production `next build` green** (`ALLOY_PROD_CERT_DIST=1`, isolated dist, in-build typecheck ON) and the upload route's `.nft.json` **confirms all runtime OCR assets are traced** (eng.traineddata, tesseract worker-script + core WASM, mupdf-wasm.wasm) → they ship on serverless. See `web/docs/sprints/active/phase-7-evidence/stage-b-ocr-deployment.md`.
- **Pre-existing baseline debt (PROVEN, not mine):** `tests/forms/ + tests/pos/` show **26 failing tests on the CLEAN staging base** (verified by `git stash -u`: base = 26 failed | 1272 passed; with my changes = the SAME 26 failed | 1276 passed — +4 from the new OCR unit tests, **zero new failures**). Label/presentation-wording drift in files outside this changeset (`outcomeConfigPresentation`, `submissionQuickReviewModal`, `structuredFormSchemaEditor`, `deriveDocumentTitle`, `questionResolutionModel`, …). Left untouched (scope discipline); flagged for a separate cleanup sprint.
- **Honest boundaries (non-goals held):** printed-text OCR only — no handwriting, no complex-table reconstruction, no perfect layout, no auto-publish. Inline OCR is correct for the governed single-document flow; a multi-page scan approaches serverless duration limits (bounded to 8 pages) — production should move OCR to an async worker/queue (documented).
- **Commit:** `afb5a99aa` · **Next:** Slice 2 — Packet Composition.

### 2026-07-25 — Slice 2 FINAL INTEGRATION — reconciled onto staging, Packet Studio converged, live completion + Processing handoff CERTIFIED

- **Reconciliation:** fetched `origin/staging`; initial **behind 47 / ahead 25**; conflict surface = the intersection of my changed files and staging's changed files = **empty**. `git rebase origin/staging` applied all 25 commits with **zero conflicts**; final **behind 0 / ahead 25**. All Slice 2 files preserved; source typecheck + Slice 2 unit suites green post-rebase. Incoming 47 commits were Director/Vacilando/Focus-Panel/Surfaces/Queue — no packet/forms/processing-composer overlap.
- **Packet Studio convergence — decision + fix:** the earlier swap was wrong (it lost definition management + broke a consolidation test). Reconciled to **one canonical creator**: `ProcessingPacketsStudio` remains the Packets tab = definition **manager** (list all defs, open/edit/reorder/activate via `ProcessingPacketBuilder`); its single **create** path now opens the requirement-responsibility composer (`PosPacketsPanel` new `composerOnly` mode); the old blank "Untitled packet" create is retired. Regression: `tests/adminV2/packetStudioConvergence.test.ts` (4) + the pre-existing `digitalMailroomFormsConsolidation` (6) green. Commit `dded1b24e`.
- **Live completion → Processing handoff CERTIFIED (`bdcb2db33`):** `phase7-packet-responsibility.spec.ts` now 2 tests green (1.3m). Second test, all real paths: create+publish form → create household customer → compose anchored packet → resolve public link → complete every step via the real public create→submit API → session `completed` → **exactly one** Processing Case via the `pos_connected` on-ramp (source_kind `form_packet_session`) → re-resolve → **no duplicate case (1/1)** → case opens via `GET /api/admin/processing/cases/[id]`. `SLICE2_HANDOFF_PASS`.
- **Definitive typechecks:** source `typecheck:build` **exit 0, 0 errors**. Test `typecheck:tests` **exit 2, 35 errors — ALL pre-existing staging queue/surfaces debt** (11 files under `tests/adminV2/runtime/*`, `tests/layout/*`, `tests/presentation/runtime/*`, from the rebased commits; intersection with my changeset = empty; files unchanged from origin/staging). Zero in Slice 2 files.
- **Regressions (post-rebase):** tests/pos 573✓/2✗, tests/forms 737✓/24✗ — the 26 are the exact pre-existing baseline (git-stash-proven; count-identical). Zero new failures.
- **Honest boundary (definitively proven, unchanged):** two DISTINCT guardian person_ids completing the SAME requirement in one family-instance session is impossible without per-recipient step handling (crm_snapshot pins person_id to first launcher; sequential single-active-step) → the named next slice (Participant Conversation Runtime integration). Those completion semantics are unit-certified (37 tests); the operator product + live single-actor completion + live handoff (one case, idempotent) are certified through real surfaces.
- **Backup:** tag `phase7-slice2-prebase` @ pre-rebase HEAD. Commits this segment: `dded1b24e`, `bdcb2db33` (+ Part 1-6 + reconcile). Branch 25 ahead / 0 behind, not pushed.

### 2026-07-25 — Slice 2 OPERATOR PRODUCT CERTIFIED (composer UI + preview + authenticated E2E green)

The requirement-responsibility model is now a **real, operator-visible product**, certified green through the real UI + real paths. Commits: `fd4b93019` (Part 1), `c5a483ba2` (Parts 2-6 UI + preview), `b6bd82ece` (mount), `d967f541e` (cert).

- **Part 1 — requirements enumeration seam:** `GET /api/admin/pos/packets/requirements` + `loadFormRequirements` + `requirementResponsibilityLabels` (operator language, valid-option sets; no raw enums/ids/JSON). 6 label tests.
- **Parts 2-6 — composer UI:** `PosPacketResponsibilityEditor` (collapsed requirement rows: label · type · responsibility summary · validation; focused editor Applies-to / Who / When in real-world language, type-based defaults, only valid options; presented content informational-only). Live household **preview** (`POST /api/admin/pos/packets/preview` + `loadPacketPreview`, representative 2-guardian/2-child household via the shared projection seam) with per-guardian views + completion summary + **blocking validation** that disables launch. Wired into `PosPacketsPanel`; rules flow to the compose payload. **Mounted** as the Studio → Packets surface (`b6bd82ece` — it was orphaned).
- **Authenticated Playwright cert GREEN** (`playwright/tests/phase7-packet-responsibility.spec.ts`, 50.8s): create+publish enrollment form (real admin API) → composer → requirements enumerate (information/upload/acknowledgement/signature) → configure (financial agreement → financial guardian) → household preview (2 guardians × 2 children, launch-ready) → blocking validation (signature → missing role blocks + disables Create, then fixed) → save (real compose) → persistence verified via the projection endpoint (financial-guardian rule round-trips). Evidence: `web/docs/sprints/active/phase-7-evidence/slice-2-responsibility/` (01-composer … 06-persisted).
- **Regression:** tests/pos 570 passed / 2 failed; tests/forms 737 passed / 24 failed — the failing 26 are the EXACT pre-existing baseline set (proven identical via git stash earlier; count-identical now), OUTSIDE this changeset. **Zero new failures.** 37 Slice-2 unit tests total green (identity/projection/completion/validation/labels). Source typecheck (tsconfig.build.json) 0 errors.
- **Runtime seam + completion + handoff:** architecturally complete + unit-certified (`projectForParticipant`, `evaluatePacketCompletion`, `processingCasePacketCompletion` idempotency) with live endpoints (`participant-projection`, on-ramp `pos_connected`). The **live multi-guardian participant-completion E2E** (both guardians complete the same requirement in one family session) is bounded by the participant-runtime work Kelly named as the NEXT slice ("Participant Conversation Runtime integration") and the explicit "do not redesign the Conversation Runtime" boundary — the single family session sequences one submission per step, so two guardians completing the same requirement is next-slice runtime work.

### 2026-07-25 — Slice 2 SERVER-COMPLETE (model + seam + endpoints, unit-certified) — UI + E2E cert REMAIN

Full requirement-responsibility **server architecture** built, tested, committed (branch 19 ahead of origin/staging). NOT yet end-to-end certified: the operator composer UI and the authenticated 12-step Playwright cert are the remaining work — deliberately NOT claimed as done (no faked cert).

**Delivered + unit-certified (25 new tests green, source+test typecheck clean):**
- Durable requirement identity `{form_definition_id, section_id?, field_id?}` (field > section > form), verified against real FormSchemaV1 (stable section/field ids, not labels/positions); `enumerateRequirementsFromForm` walks a published form into typed, stably-identified requirements with sensible editable defaults. `17ec8ad9a`.
- Projection seam `projectPacketResponsibilities` + `evaluatePacketCompletion`/`isPacketComplete` + `projectForParticipant` (the Conversation Runtime seam) — the single source; UI/API/runtime never re-resolve. Blocking validation prevents launch. `e5a8cd58a`.
- Compose route persists operator rules on packet metadata; `pos_connected: true` set (on-ramp repair). `GET /api/admin/pos/packets/[id]/projection` (operator preview + read-model projection). `2467bd67a`.
- `GET /api/admin/pos/packets/sessions/[sessionId]/participant-projection?person_id=` — per-guardian runtime seam + packet completion from REAL session submissions. `a1878d15b`.
- Design doc `docs/platform/planning/phase7-requirement-responsibility-model.md` (`456b992c7`); `pos_connected` repair (`456b992c7`).

**Files:** `lib/pos/packet/{requirementResponsibility,packetResponsibilityProjection,loadPacketProjection,loadParticipantProjection}.ts`; routes `pos/packets/compose`, `pos/packets/[packetDefinitionId]/projection`, `pos/packets/sessions/[sessionId]/participant-projection`; tests `tests/pos/{requirementResponsibility,packetResponsibilityProjection}.test.ts`. No DB migration (rules ride packet metadata).

**REMAINING for "walkable + certified" (next focused build):** (1) `GET requirements` enumeration endpoint for a set of form ids (so the composer can show per-requirement controls pre-compose); (2) operator composer UI — author applies-to / responsible-party / satisfaction per requirement in real-world language, with type-based defaults; (3) household projection **preview** UI + block-launch on blocking issues; (4) authoritative completion wiring into the packet session (currently form-submitted drives session status; requirement-completion is computed/exposed via the seam); (5) the 12-step authenticated Playwright cert (compose→configure→preview→persist→launch→per-guardian projection→completion→one Processing Case) + evidence; (6) regression suite. Commits: `456b992c7`, `17ec8ad9a`, `e5a8cd58a`, `2467bd67a`, `a1878d15b`.

### 2026-07-25 — Slice 2 FOUNDATION — requirement-responsibility model + pos_connected repair (commit `456b992c7`)

Product reframe accepted (Kelly): **stop thinking packet/item assignment; think requirement responsibility.** Hierarchy `Packet → Forms → Requirements → Responsibility Rules → Conversation Runtime`. Packets keep composing forms; obligation-bearing elements (upload/ack/signature/static/generated) live inside forms as section dispositions (Slice 1); packet views + participant journeys + conversation runtime + completion are all derived projections of responsibility rules. No second assignment architecture to migrate away from.

- **`pos_connected` repair (independent bug, done):** `/api/admin/pos/packets/compose` now sets `metadata.pos_connected: true` on the packet definition, so completing a composer packet opens a Processing Case (the on-ramp gates on `isPosConnectedSurface`). Previously composer packets never tripped the marker → journey dead-ended at completion. *Live completion→Processing verification deferred to the participant-journey cert.*
- **Design:** `docs/platform/planning/phase7-requirement-responsibility-model.md` — the three stable axes (applies_to / responsible_party / satisfied_by), requirement ref `{form_definition_id, section_key?}`, resolution order (section → form-default → packet-default → built-in), rules ride packet `metadata.requirement_responsibilities` now and promote to a first-class table later with **no axis/projection change**.
- **Deterministic core (`lib/pos/packet/requirementResponsibility.ts`, pure):** the axis types; `resolveResponsibility`; `deriveParticipantRequirements` (fans requirements over children/guardians into concrete instances — one per child for child-scope, one family instance otherwise; resolves responsible participants incl. either/all/specific/financial guardian, child, role); `evaluateCompletion` (per satisfaction rule, reports outstanding); `parseRequirementResponsibilityRules` (safe metadata round-trip).
- **Tests/evidence:** `tests/pos/requirementResponsibility.test.ts` 14 green — household vs child-specific, either/both/specific/financial guardian, signature/ack/upload ownership, resolution order, completion per rule, metadata parse. Source + test typecheck clean (0).
- **Scope held (per direction):** did NOT build the generalized responsibility engine or first-class ack/signature packet-item types. Minimum-now, expands-later.
- **Commit:** `456b992c7` · **Next (Slice 2 continued):** wire `/compose` to author + persist responsibility rules; surface projected requirements/completion in the packet read model + operator view; then the Conversation Runtime projects per-participant work from the same seam; walk + cert the enrollment proving journey (parent receives packet → completes conversationally → submits through Processing).

<!-- Append one checkpoint per slice: outcome now walkable · files/changes · tests/evidence · defects found · deferred non-blockers · commit · next slice -->
