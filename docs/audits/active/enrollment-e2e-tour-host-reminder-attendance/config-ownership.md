# Tour communications config ownership (audit note)

Superseded by `docs/sprints/active/tour-comms-internal-recipients-config-ownership.md`.

Internal Tour calendar recipients are configured via:

```json
{ "tour_comms": { "internal_recipients": { "enabled": true, "user_ids": ["…"] } } }
```

No Tour Host assignment model in this sprint.
