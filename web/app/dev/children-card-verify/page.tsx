import ChildrenCardVerify from "@/app/dev/children-card-verify/ChildrenCardVerify";

/** Dev-only, no-auth harness for the ChildrenCard Universal Card Lifecycle. */
export default function ChildrenCardVerifyPage() {
    return <ChildrenCardVerify />;
}
