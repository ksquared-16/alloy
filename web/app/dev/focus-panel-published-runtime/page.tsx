import FocusPanelPublishedRuntimeDevClient from "@/app/dev/focus-panel-published-runtime/FocusPanelPublishedRuntimeDevClient";

/** Dev-only, no-auth harness: the RUNTIME published Focus Panel grid (work-unit parity). */
export default function FocusPanelPublishedRuntimeDevPage() {
    return <FocusPanelPublishedRuntimeDevClient />;
}
