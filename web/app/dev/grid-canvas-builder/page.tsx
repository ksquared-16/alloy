import GridCanvasBuilderDevClient from "@/app/dev/grid-canvas-builder/GridCanvasBuilderDevClient";

/** Dev-only, no-auth harness for the EB V5 responsive grid canvas builder + runtime. */
export default function GridCanvasBuilderDevPage() {
    return <GridCanvasBuilderDevClient />;
}
