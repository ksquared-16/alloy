import ChildInspectorVerify from "@/app/dev/child-inspector-verify/ChildInspectorVerify";

/** Dev-only, no-auth harness for the Child card Evidence Group inspector. */
export default function ChildInspectorVerifyPage() {
    return <ChildInspectorVerify />;
}
