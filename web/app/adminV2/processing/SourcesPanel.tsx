"use client";

/**
 * DEPRECATED — superseded by the POS left-nav "Sources" group (Forms / Packets /
 * Documents) in `PosWorkspaceLayout`.
 *
 * The old single "Sources" tab deep-linked to the legacy `/admin/forms` app. Those
 * surfaces are now native POS sections. This stub renders nothing and is not mounted
 * anywhere; it remains only because the workspace mount disallows file deletion.
 */

export default function SourcesPanel() {
    return null;
}
