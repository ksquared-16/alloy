"use client";

/**
 * Alias of the canonical Classification panel (`processing/ClassificationPanel`,
 * POS-FP9 Sprint 2.5) so POS columns and the standalone drawer render one identical
 * panel. Kept as a re-export to avoid churn across existing POS imports.
 */

export { default } from "@/app/adminV2/processing/ClassificationPanel";
