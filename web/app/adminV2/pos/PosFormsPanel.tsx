"use client";

/**
 * DEPRECATED — superseded by `PosFormsWorkspace`.
 *
 * The old POS Forms panel deep-linked to the legacy `/admin/forms` app. Forms are
 * now native inside POS (list → builder canvas → palette/properties → preview) via
 * `PosFormsWorkspace`. This stub remains only to avoid a hard delete on a mount that
 * disallows file removal; it renders nothing and is not mounted anywhere.
 */

export default function PosFormsPanel() {
    return null;
}
