import FormDetailClient from "@/app/legacy-admin/forms/[formId]/FormDetailClient";

export const dynamic = "force-dynamic";

export default function AdminV2FormDetailPage() {
  return <FormDetailClient />;
}
