import ComingSoonPlaceholder from "@/components/admin/ComingSoonPlaceholder";

export default function AdminOperationsRecurrencePage() {
    return (
        <ComingSoonPlaceholder
            title="Recurrence"
            description="Job-level operational recurrence config (recurrence_plans). Configure how often recurring jobs repeat."
        />
    );
}
