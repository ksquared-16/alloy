import ComingSoonPlaceholder from "@/components/admin/ComingSoonPlaceholder";

export default function AdminSystemRolesPage() {
    return (
        <ComingSoonPlaceholder
            title="Roles & Permissions"
            description="Define roles and fine-grained permissions for admin and ops users. Next phase after nav restructure."
        />
    );
}
