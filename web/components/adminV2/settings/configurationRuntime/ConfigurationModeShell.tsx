/**
 * Reusable Configuration Mode shell: Queue → list column → workspace.
 * Used by Processes today; Layouts and other surfaces adopt the same pattern next.
 */
export { default } from "@/components/adminV2/settings/businessProcess/BusinessProcessConfigurationShell";
