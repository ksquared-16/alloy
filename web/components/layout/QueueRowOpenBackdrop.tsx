"use client";

/** Decorative full-row layer — opportunity open is handled by content click delegation. */
export default function QueueRowOpenBackdrop() {
    return <div className="operational-queue-row__open-backdrop" aria-hidden="true" />;
}
