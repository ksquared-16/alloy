.next-prodcert/

# Dependencies — directory OR symlink (see root .gitignore for why both forms).
node_modules
node_modules/

# R11 measurement working state (raw subject ids — never committed)
.r11-out/
