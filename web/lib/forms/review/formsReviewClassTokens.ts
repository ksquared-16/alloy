/**
 * Forms/Documents review class tokens.
 *
 * Re-exports the shared operational visual system for backward-compatible imports.
 * New code may import from `@/lib/operational/ui/operationalVisualTokens` directly.
 */

export {
    opCaseFileCanvas as formsCaseFileCanvas,
    opCaseFileCanvasCompact as formsCaseFileCanvasCompact,
    opRegionSeparator as formsCaseFileRegionSeparator,
    opGroupedRowInner as formsCaseFileGroupedRowInner,
    opStackRegion as formsCaseFileStack,
    opStackRegionCompact as formsCaseFileStackCompact,
    opOrientationSurface as formsCaseFileHeaderSurface,
    opCaseFileTitle as formsCaseFileHeaderTitle,
    opCaseFileSubtitle as formsCaseFileHeaderSubtitle,
    opReviewActionsSurface as formsCaseFileReviewActionsSurface,
    opStepCardSurface as formsCaseFileStepCard,
    opSectionTitle as formsCaseFileRegionTitle,
    opSectionSupport as formsCaseFileRegionDescription,
    opBody as formsCaseFileBodyText,
    opMetadata as formsCaseFileMetaText,
    opActionLink as formsCaseFileActionLink,
    opGroupedSurface as formsCaseFileGroupedSurface,
    opGroupedRow as formsCaseFileGroupedRow,
    opDivider as formsCaseFileDivider,
    opIntakeArtifactRow as formsIntakeArtifactCard,
    opLabelCapsSoft as formsIntakeArtifactKindLabel,
    opProvenanceOrigin as formsIntakeProvenanceOrigin,
    opProvenanceMeta as formsIntakeProvenanceMeta,
    opProvenanceLegend as formsIntakeArtifactLegend,
    opAnswerSurface as formsCaseFileAnswerSurface,
    opIntelligenceSurface as formsBosAssistSurface,
    opLabelCaps as formsBosAssistSubheading,
    opInsightBulletList as formsBosAssistBulletList,
    opInsightAuthorityNote as formsBosAssistAuthorityNote,
} from "@/lib/operational/ui/operationalVisualTokens";
