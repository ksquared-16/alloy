/**
 * Supabase Storage bucket for org-scoped documents, including the public
 * "Join Our Team" vendor application uploads (proof of insurance, driver's license).
 *
 * Single source of truth — do not duplicate the string elsewhere for this flow.
 */
export const ORG_DOCUMENTS_STORAGE_BUCKET = "org_documents";
