/**
 * Book-v2 catalog resolution: stable option keys (option_sets) replace legacy FK lookups.
 */

export { homeTypeInputToStableKey, uiAccessMethodToStableKey } from "@/lib/book-v2/bookingCanonicalMaps";
