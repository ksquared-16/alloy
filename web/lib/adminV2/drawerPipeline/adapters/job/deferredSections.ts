/** Below-fold sections — chrome reserved; values hydrate without reshaping above-fold. */
export const JOB_DEFERRED_OVERVIEW_SECTION_KEYS = new Set<string>(["communications_canonical_embed"]);

export const JOB_PRIMARY_SHELL_ATTACHES = [] as const;
