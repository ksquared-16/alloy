import type { DrawerTabKey } from "@/lib/entityPresentation";

/** Stable workflow v1 drawer tabs — must not be discovered after open. */
export const OPPORTUNITY_INQUIRY_WORKFLOW_TAB_STRIP: DrawerTabKey[] = [
    "overview",
    "communications",
    "notes",
    "documents",
    "activity",
];
