/**
 * Experience Builder field authoring helpers — Sprint 5.18E.
 */

/** Frequently used opportunity-drawer fields surfaced at the top of the picker. */
export const EXPERIENCE_BUILDER_COMMON_FIELD_REF_KEYS = [
    "person.primary_contact_name",
    "person.primary_email",
    "person.primary_phone",
    "child.name",
    "child.first_name",
    "child.last_name",
    "child.age",
    "inquiry_child.desired_program_type",
    "inquiry_child.desired_start_date",
    "customer.name",
] as const;
