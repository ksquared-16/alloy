/** Stable key for opportunity drawer lifecycle band + `GET …/actions?section_key=…`. Must match seeded `action_placements.section_key`. */
export const OPPORTUNITY_RECORD_SECTION_LIFECYCLE = "opportunity_lifecycle" as const;
