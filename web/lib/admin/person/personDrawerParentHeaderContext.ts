/** Parent overview section titles — code-driven until layout config ships. */
export function personDrawerParentSectionTitle(sectionKey: string, defaultTitle: string): string {
    return defaultTitle;
}
