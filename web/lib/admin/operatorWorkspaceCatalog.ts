/**
 * Operator workspace route constants (non-lifecycle — lifecycle cards are catalog-driven).
 */

export const OPERATOR_TASKS_HREF = "/admin/tasks" as const;
export const OPERATOR_INBOX_HREF = "/admin/messages" as const;
