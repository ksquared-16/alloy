#!/usr/bin/env bash
# Cleanup wrapper — requires --confirm (see alloy-cert-leak-report).
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${SCRIPT_DIR}/alloy-cert-leak-report" clean "$@"
