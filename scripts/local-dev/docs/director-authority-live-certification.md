# Director Authority — live certification marker

This file exists so a routine managed-branch push can be certified end to end
against the installed Gateway on a commit with no pre-existing authorization.

The property under certification: a Director-approved routine push reaches
COMPLETE without ever returning to the operator's approval bar.
